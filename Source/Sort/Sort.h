#include <vector>

using namespace std;

template <typename T> void printVector(vector<T> &input);
template <typename T> void bubbleSort(vector<T> &input);
template <typename T> void selectionSort(vector<T>& input);
void countingSort(vector<int>& input, int min, int max);
